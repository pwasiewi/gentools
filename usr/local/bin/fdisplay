#!/bin/bash
# fdisplay - find active DISPLAY for the current user
ps -u "$(id -u)" -o pid= | while read -r pid; do
  tr '\0' '\n' < /proc/"$pid"/environ 2>/dev/null | grep '^DISPLAY=:'
done | grep -o ':[0-9]*' | sort -u
